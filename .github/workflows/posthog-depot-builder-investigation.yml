name: "PostHog - Depot Builder Investigation"
run-name: >-
  ${{ format('Depot Builder Investigation | {0}', github.ref_name) }}

on:
  workflow_dispatch:
    inputs:
      runner_label:
        description: "GitHub runner label that launches the Depot remote build"
        required: true
        type: string
        default: ubuntu-latest
      project_id:
        description: "Depot project ID. Defaults to vars.DEPOT_PROJECT_ID."
        required: false
        type: string
        default: ''
      auth_mode:
        description: "Depot auth mode. OIDC is preferred; token uses secrets.DEPOT_TOKEN."
        required: true
        type: choice
        default: token
        options:
          - token
          - oidc
      cache_backend:
        description: "Depot cache only, or also export/import an external GitHub Actions cache."
        required: true
        type: choice
        default: depot
        options:
          - depot
          - gha

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write
  actions: write

jobs:
  depot-build:
    name: Docker Build (Depot builder)
    runs-on: ${{ inputs.runner_label }}
    timeout-minutes: 120
    env:
      DEPOT_PROJECT_ID: ${{ inputs.project_id || vars.DEPOT_PROJECT_ID }}
      DEPOT_CACHE_SCOPE: ${{ format('posthog-depot-builder-{0}', github.ref_name) }}
    steps:
      - name: Validate Depot project
        run: |
          set -euo pipefail
          if [[ -z "${DEPOT_PROJECT_ID}" ]]; then
            echo "DEPOT_PROJECT_ID is required. Set the repository variable or pass project_id." >&2
            exit 1
          fi

      - name: Checkout benchmark repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          submodules: false

      - name: Checkout pinned upstream submodule shallowly
        run: |
          set -euo pipefail
          git submodule sync --recursive
          git -c protocol.version=2 submodule update --init --depth 1 --recommend-shallow --jobs 4 upstream

      - name: Prepare base benchmark source tree
        run: ./scripts/prepare-source.sh base

      - name: Set up Depot CLI
        uses: depot/setup-action@v1

      - name: Capture upstream ref
        id: upstream
        run: echo "sha=$(git rev-parse HEAD:upstream)" >> "$GITHUB_OUTPUT"

      - name: Start build timing
        id: timing_start
        run: echo "started_at=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Build Docker image with Depot (OIDC)
        if: inputs.auth_mode == 'oidc' && inputs.cache_backend == 'depot'
        id: depot_build_oidc
        uses: depot/build-push-action@v1
        with:
          project: ${{ env.DEPOT_PROJECT_ID }}
          context: upstream
          file: upstream/Dockerfile
          push: false
          provenance: false
          buildx-fallback: false

      - name: Build Docker image with Depot (token)
        if: inputs.auth_mode == 'token' && inputs.cache_backend == 'depot'
        id: depot_build_token
        uses: depot/build-push-action@v1
        with:
          project: ${{ env.DEPOT_PROJECT_ID }}
          token: ${{ secrets.DEPOT_TOKEN }}
          context: upstream
          file: upstream/Dockerfile
          push: false
          provenance: false
          buildx-fallback: false

      - name: Build Docker image with Depot and GHA cache export (OIDC)
        if: inputs.auth_mode == 'oidc' && inputs.cache_backend == 'gha'
        id: depot_build_oidc_gha
        uses: depot/build-push-action@v1
        with:
          project: ${{ env.DEPOT_PROJECT_ID }}
          context: upstream
          file: upstream/Dockerfile
          push: false
          provenance: false
          buildx-fallback: false
          cache-from: type=gha,scope=${{ env.DEPOT_CACHE_SCOPE }}
          cache-to: type=gha,mode=max,scope=${{ env.DEPOT_CACHE_SCOPE }}

      - name: Build Docker image with Depot and GHA cache export (token)
        if: inputs.auth_mode == 'token' && inputs.cache_backend == 'gha'
        id: depot_build_token_gha
        uses: depot/build-push-action@v1
        with:
          project: ${{ env.DEPOT_PROJECT_ID }}
          token: ${{ secrets.DEPOT_TOKEN }}
          context: upstream
          file: upstream/Dockerfile
          push: false
          provenance: false
          buildx-fallback: false
          cache-from: type=gha,scope=${{ env.DEPOT_CACHE_SCOPE }}
          cache-to: type=gha,mode=max,scope=${{ env.DEPOT_CACHE_SCOPE }}

      - name: Write investigation artifact
        if: always()
        env:
          STARTED_AT: ${{ steps.timing_start.outputs.started_at }}
          UPSTREAM_REF: ${{ steps.upstream.outputs.sha }}
          AUTH_MODE: ${{ inputs.auth_mode }}
          CACHE_BACKEND: ${{ inputs.cache_backend }}
          BUILD_ID: ${{ steps.depot_build_token.outputs['build-id'] || steps.depot_build_oidc.outputs['build-id'] || steps.depot_build_token_gha.outputs['build-id'] || steps.depot_build_oidc_gha.outputs['build-id'] }}
          PROJECT_ID: ${{ steps.depot_build_token.outputs['project-id'] || steps.depot_build_oidc.outputs['project-id'] || steps.depot_build_token_gha.outputs['project-id'] || steps.depot_build_oidc_gha.outputs['project-id'] }}
          IMAGE_ID: ${{ steps.depot_build_token.outputs.imageid || steps.depot_build_oidc.outputs.imageid || steps.depot_build_token_gha.outputs.imageid || steps.depot_build_oidc_gha.outputs.imageid }}
          DIGEST: ${{ steps.depot_build_token.outputs.digest || steps.depot_build_oidc.outputs.digest || steps.depot_build_token_gha.outputs.digest || steps.depot_build_oidc_gha.outputs.digest }}
        run: |
          set -euo pipefail
          ended_at="$(date +%s)"
          started_at="${STARTED_AT:-$ended_at}"
          build_seconds="$((ended_at - started_at))"
          mkdir -p benchmark-depot-builder
          jq -n \
            --arg project_repo "PostHog/posthog" \
            --arg project_ref "${UPSTREAM_REF}" \
            --arg strategy "depot-builder" \
            --arg runner_label "${RUNNER_NAME:-${{ inputs.runner_label }}}" \
            --arg auth_mode "${AUTH_MODE}" \
            --arg cache_backend "${CACHE_BACKEND}" \
            --arg cache_scope "${DEPOT_CACHE_SCOPE}" \
            --arg depot_project_id "${PROJECT_ID:-${DEPOT_PROJECT_ID}}" \
            --arg depot_build_id "${BUILD_ID}" \
            --arg image_id "${IMAGE_ID}" \
            --arg digest "${DIGEST}" \
            --argjson build_seconds "${build_seconds}" \
            '{
              project: {repo: $project_repo, ref: $project_ref},
              strategy: $strategy,
              runner_label: $runner_label,
              depot: {
                auth_mode: $auth_mode,
                cache_backend: $cache_backend,
                cache_scope: $cache_scope,
                project_id: $depot_project_id,
                build_id: $depot_build_id,
                image_id: $image_id,
                digest: $digest
              },
              runs: {
                depot_build_seconds: $build_seconds
              }
            }' > benchmark-depot-builder/posthog-depot-builder.json
          {
            echo "### PostHog Depot builder investigation"
            echo
            echo "- upstream ref: \`${UPSTREAM_REF}\`"
            echo "- Depot build seconds: \`${build_seconds}\`"
            echo "- Depot project: \`${PROJECT_ID:-${DEPOT_PROJECT_ID}}\`"
            echo "- Depot build id: \`${BUILD_ID:-unknown}\`"
            echo "- cache backend: \`${CACHE_BACKEND}\`"
            echo "- cache scope: \`${DEPOT_CACHE_SCOPE}\`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload investigation artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-posthog-depot-builder
          path: benchmark-depot-builder/
          if-no-files-found: error
