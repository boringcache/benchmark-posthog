name: "PostHog - BoringCache"
run-name: >-
  ${{ format('{0} | {1}', github.event_name == 'workflow_dispatch' && inputs.cache_lane == 'rolling' && 'BC Rolling' || 'BC Fresh', github.event_name == 'push' && github.event.head_commit.message || github.event_name == 'pull_request' && format('PR #{0}', github.event.pull_request.number) || format('Manual dispatch ({0})', github.ref_name)) }}

on:
  workflow_dispatch:
    inputs:
      cache_lane:
        description: "Benchmark lane"
        required: false
        type: choice
        default: fresh
        options:
          - fresh
          - rolling
      cli_ref:
        description: "Optional boringcache/cli ref to build and use instead of the released CLI"
        required: false
        type: string
      turbo_backend:
        description: "How BoringCache integrates with Turbo in benchmark builds"
        required: false
        default: proxy
        type: choice
        options:
          - proxy
          - archive
          - off
  push:
    branches:
      - main
    paths:
      - ".gitmodules"
      - "upstream"
  pull_request:
    paths:
      - ".dockerignore"
      - ".gitmodules"
      - "Dockerfile.benchmark"
      - ".github/workflows/posthog-boringcache.yml"
      - ".github/workflows/posthog-actions-cache.yml"
      - "README.md"
      - "scenarios/**"
      - "scripts/**"
      - "upstream"

permissions:
  contents: read
  actions: read

env:
  PROJECT_REPO: PostHog/posthog
  BENCHMARK_ID: posthog
  BENCHMARK_WORKSPACE: boringcache/benchmarks
  TURBO_BACKEND: ${{ github.event.inputs.turbo_backend || 'proxy' }}

jobs:
  seed-cache:
    name: ${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane == 'rolling' && 'Docker Seed (BoringCache, rolling)' || 'Docker Seed (BoringCache, fresh)' }}
    runs-on: ubuntu-latest
    timeout-minutes: 180
    outputs:
      cache_scope: ${{ steps.scope.outputs.cache_scope }}
      cold_seconds: ${{ steps.cold_baseline_build.outputs.seconds }}
      docker_cache_export_seconds: ${{ steps.cold_baseline_build.outputs.docker_cache_export_seconds }}
      tags_csv: ${{ steps.scope.outputs.tags_csv }}
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          df -h /

      - name: Validate BoringCache secrets
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_SAVE_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}
        run: |
          set -euo pipefail
          if [[ -z "${BORINGCACHE_RESTORE_TOKEN}" ]]; then
            echo "Missing BORINGCACHE_RESTORE_TOKEN secret" >&2
            exit 1
          fi
          if [[ -z "${BORINGCACHE_SAVE_TOKEN}" ]]; then
            echo "Missing BORINGCACHE_SAVE_TOKEN secret" >&2
            exit 1
          fi

      - name: Checkout benchmark repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          submodules: false

      - name: Checkout pinned upstream submodule shallowly
        run: |
          set -euo pipefail
          git submodule sync --recursive
          git -c protocol.version=2 submodule update --init --depth 1 --recommend-shallow --jobs 4 upstream

      - name: Prepare base benchmark source tree
        run: ./scripts/prepare-source.sh base

      - name: Install BoringCache CLI with boringcache/one
        uses: boringcache/one@v1
        with:
          setup: none
          mode: archive
          workspace: ${{ env.BENCHMARK_WORKSPACE }}
          cli-platform: linux-amd64
          cache-runtime: false

      - name: Prepare benchmark build inputs
        id: scope
        run: |
          cp "$(which boringcache)" ./boringcache-bin

          ref_slug="$(printf '%s' "${GITHUB_REF_NAME}" | tr -c 'A-Za-z0-9._-' '-')"
          ref_slug="${ref_slug#-}"
          ref_slug="${ref_slug%-}"
          ref_slug="${ref_slug:-main}"
          if [[ "${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane || 'fresh' }}" == "rolling" ]]; then
            run_suffix="rolling-${ref_slug}"
          else
            run_suffix="r${GITHUB_RUN_ID}-a${GITHUB_RUN_ATTEMPT}"
          fi
          cache_scope="${BENCHMARK_ID}-run-${run_suffix}"
          docker_registry_tag="${BENCHMARK_ID}-docker-layers-${run_suffix}"
          frontend_pnpm_tag="posthog-pnpm-frontend-${run_suffix}"
          plugin_pnpm_tag="posthog-pnpm-plugin-${run_suffix}"
          case "${TURBO_BACKEND}" in
            archive)
              turbo_tag="posthog-turbo-archive-${run_suffix}"
              turbo_plugin_tag="posthog-turbo-plugin-archive-${run_suffix}"
              ;;
            proxy|off)
              turbo_tag="posthog-turbo-${run_suffix}"
              turbo_plugin_tag="posthog-turbo-plugin-${run_suffix}"
              ;;
            *)
              echo "Unknown TURBO_BACKEND: ${TURBO_BACKEND}" >&2
              exit 1
              ;;
          esac
          node_scripts_tag="posthog-node-scripts-${run_suffix}"
          python_tag="posthog-python-${run_suffix}"
          staticfiles_tag="posthog-staticfiles-${run_suffix}"
          playwright_tag="posthog-playwright-${run_suffix}"
          tags_csv="${frontend_pnpm_tag},${plugin_pnpm_tag},${turbo_tag},${turbo_plugin_tag},${node_scripts_tag},${python_tag},${staticfiles_tag},${playwright_tag},${cache_scope},${docker_registry_tag}"
          registry_proxy_tags="${cache_scope},${docker_registry_tag}"
          echo "cache_scope=${cache_scope}" >> "$GITHUB_OUTPUT"
          echo "docker_registry_tag=${docker_registry_tag}" >> "$GITHUB_OUTPUT"
          echo "registry_proxy_tags=${registry_proxy_tags}" >> "$GITHUB_OUTPUT"
          echo "frontend_pnpm_tag=${frontend_pnpm_tag}" >> "$GITHUB_OUTPUT"
          echo "plugin_pnpm_tag=${plugin_pnpm_tag}" >> "$GITHUB_OUTPUT"
          echo "turbo_tag=${turbo_tag}" >> "$GITHUB_OUTPUT"
          echo "turbo_plugin_tag=${turbo_plugin_tag}" >> "$GITHUB_OUTPUT"
          echo "node_scripts_tag=${node_scripts_tag}" >> "$GITHUB_OUTPUT"
          echo "python_tag=${python_tag}" >> "$GITHUB_OUTPUT"
          echo "staticfiles_tag=${staticfiles_tag}" >> "$GITHUB_OUTPUT"
          echo "playwright_tag=${playwright_tag}" >> "$GITHUB_OUTPUT"
          echo "tags_csv=${tags_csv}" >> "$GITHUB_OUTPUT"
          cat > ./run-boringcache-build.sh <<'SH'
          #!/usr/bin/env bash
          set -euo pipefail

          proxy_port="${BORINGCACHE_PROXY_PORT:-5000}"
          proxy_log="${BORINGCACHE_PROXY_LOG_PATH:-/tmp/boringcache-proxy-${proxy_port}.log}"
          build_log="$(mktemp /tmp/boringcache-build.XXXXXX.log)"
          max_attempts=3
          cache_export_pattern='expected sha256:.*got sha256:e3b0|error writing layer blob|400 Bad Request|broken pipe'

          mode="${1:-full}"
          start_proxy() { :; }
          stop_proxy() { :; }
          wait_proxy_ready() { return 0; }
          cleanup() { :; }
          trap cleanup EXIT

          find_step_id() {
            local pattern="$1"
            sed -nE "s/^#([0-9]+) ${pattern}.*/\\1/p" "$build_log" | tail -n1
          }

          find_step_seconds() {
            local step_id="$1"
            [[ -n "$step_id" ]] || return 0
            sed -nE "s/^#${step_id} DONE ([0-9]+(\\.[0-9]+)?)s$/\\1/p" "$build_log" | tail -n1
          }

          write_build_metrics() {
            local output_path="${BENCHMARK_METRICS_OUTPUT:-}"
            [[ -n "$output_path" ]] || return 0

            local import_step=""
            local export_step=""
            local import_seconds=""
            local export_seconds=""

            import_step="$(find_step_id "importing cache manifest from")"
            export_step="$(find_step_id "exporting cache to registry")"
            import_seconds="$(find_step_seconds "$import_step")"
            export_seconds="$(find_step_seconds "$export_step")"

            mkdir -p "$(dirname "$output_path")"
            : > "$output_path"
            if [[ -n "$import_seconds" ]]; then
              echo "docker_cache_import_seconds=$import_seconds" >> "$output_path"
            fi
            if [[ -n "$export_seconds" ]]; then
              echo "docker_cache_export_seconds=$export_seconds" >> "$output_path"
            fi
          }

          attempt=1
          while true; do
            cache_args=()
            internal_restore_enabled="1"
            internal_save_enabled="1"
            turbo_proxy_enabled="${BORINGCACHE_TURBO_PROXY_ENABLED:-1}"
            if [[ "$mode" == "full" ]]; then
              [[ -n "${CACHE_FROM:-}" ]] && cache_args+=(--cache-from "$CACHE_FROM")
              [[ -n "${CACHE_TO:-}" ]] && cache_args+=(--cache-to "$CACHE_TO")
            elif [[ "$mode" == "seed-cache" ]]; then
              # --no-cache is required for type=registry export: without it, buildx
              # sees cached layers from the builder and skips pushing blobs to the
              # registry proxy, so the proxy never uploads to BC backend.
              cache_args=(--no-cache)
              [[ -n "${CACHE_TO:-}" ]] && cache_args+=(--cache-to "$CACHE_TO")
            elif [[ "$mode" == "partial-warm" ]]; then
              # Read-only: no --cache-to. Internal saves are gated by an empty
              # save-token secret so the Dockerfile instruction keys stay stable.
              [[ -n "${CACHE_FROM:-}" ]] && cache_args+=(--cache-from "$CACHE_FROM")
            elif [[ "$mode" == "internal-only" ]]; then
              # Disable Docker layer cache for a full stale rebuild.
              cache_args=(--no-cache)
            elif [[ "$mode" == "internal-target" ]]; then
              # Isolate internal dependency/output cache impact without final-image OS package work.
              cache_args=(--no-cache --target posthog-build)
            else
              echo "Unknown build mode: $mode" >&2
              exit 1
            fi
            start_proxy
            if ! wait_proxy_ready; then
              echo "Registry proxy did not become ready (attempt ${attempt}/${max_attempts})" >&2
              tail -n 200 "$proxy_log" || true
              if [[ "$attempt" -ge "$max_attempts" ]]; then
                exit 1
              fi
              stop_proxy
              attempt=$((attempt + 1))
              sleep 3
              continue
            fi

            : > "$build_log"
            set +e
            DOCKER_BUILDKIT=1 docker buildx build \
              --builder "$BUILDER" \
              --file Dockerfile.benchmark \
              --tag "$IMAGE_TAG" \
              --progress=plain \
              "${cache_args[@]}" \
              --secret id=boringcache_token,env=BORINGCACHE_RESTORE_TOKEN \
              --secret id=boringcache_save_token,env=BORINGCACHE_SAVE_TOKEN \
              --build-arg BORINGCACHE_WORKSPACE="$BENCHMARK_WORKSPACE" \
              --build-arg BORINGCACHE_TAG_PNPM_FRONTEND="$BORINGCACHE_TAG_PNPM_FRONTEND" \
              --build-arg BORINGCACHE_TAG_PNPM_PLUGIN="$BORINGCACHE_TAG_PNPM_PLUGIN" \
              --build-arg BORINGCACHE_TAG_TURBO="$BORINGCACHE_TAG_TURBO" \
              --build-arg BORINGCACHE_TAG_TURBO_PLUGIN="$BORINGCACHE_TAG_TURBO_PLUGIN" \
              --build-arg BORINGCACHE_TAG_NODE_SCRIPTS="$BORINGCACHE_TAG_NODE_SCRIPTS" \
              --build-arg BORINGCACHE_TAG_PYTHON="$BORINGCACHE_TAG_PYTHON" \
              --build-arg BORINGCACHE_TAG_STATICFILES="$BORINGCACHE_TAG_STATICFILES" \
              --build-arg BORINGCACHE_TAG_PLAYWRIGHT="$BORINGCACHE_TAG_PLAYWRIGHT" \
              --build-arg BORINGCACHE_INTERNAL_RESTORE_ENABLED="$internal_restore_enabled" \
              --build-arg BORINGCACHE_INTERNAL_SAVE_ENABLED="$internal_save_enabled" \
              --build-arg BORINGCACHE_TURBO_PROXY_ENABLED="$turbo_proxy_enabled" \
              --build-arg BORINGCACHE_TURBO_BACKEND="${TURBO_BACKEND}" \
              . 2>&1 | tee "$build_log"
            status=${PIPESTATUS[0]}
            set -e

            if [[ "$status" -eq 0 ]]; then
              if [[ "$mode" =~ ^(full|seed-cache)$ ]] && grep -Eq "$cache_export_pattern" "$build_log"; then
                stop_proxy
                if [[ "$attempt" -lt "$max_attempts" ]]; then
                  echo "Build succeeded but cache export was degraded (attempt ${attempt}/${max_attempts}); retrying to self-heal cache..." >&2
                  attempt=$((attempt + 1))
                  sleep 3
                  continue
                fi
                echo "Build succeeded but cache export still degraded after ${max_attempts} attempts; continuing run with best-effort cache state." >&2
              fi
              # Stop proxy gracefully so it can flush pending uploads
              if [[ "$mode" =~ ^(seed-cache|full)$ ]]; then
                echo "Flushing proxy cache to backend..."
                stop_proxy
              fi
              # Dump proxy log for diagnostics
              echo "=== Proxy log (${mode}, last 200 lines) ==="
              tail -n 200 "$proxy_log" 2>/dev/null || true
              echo "=== End proxy log ==="
              write_build_metrics
              break
            fi

            stop_proxy

            if [[ "$attempt" -ge "$max_attempts" ]]; then
              echo "Build (${mode}) failed after ${max_attempts} attempts" >&2
              tail -n 200 "$build_log" || true
              tail -n 400 "$proxy_log" || true
              exit "$status"
            fi

            if grep -Eq "$cache_export_pattern" "$build_log"; then
              echo "Detected registry blob finalize mismatch (attempt ${attempt}/${max_attempts}); restarting proxy and retrying..." >&2
            else
              echo "Build (${mode}) failed (attempt ${attempt}/${max_attempts}); retrying..." >&2
            fi

            attempt=$((attempt + 1))
            sleep 3
          done
          SH
          chmod +x ./run-boringcache-build.sh

      - name: Configure boringcache/one for Docker
        id: docker_cache
        uses: boringcache/one@v1
        with:
          cli-version: skip
          setup: none
          mode: docker
          docker-command: setup
          workspace: ${{ env.BENCHMARK_WORKSPACE }}
          cache-tag: ${{ steps.scope.outputs.cache_scope }}
          registry-tag: ${{ steps.scope.outputs.registry_proxy_tags }}
          proxy-no-git: 'true'
          driver-opts: |
            network=host
          verbose: true
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_SAVE_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}

      - name: ${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane == 'rolling' && 'Run rolling seed build' || 'Run fresh seed build' }}
        id: cold_baseline_build
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_SAVE_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}
          BORINGCACHE_PROXY_LOG_PATH: ${{ steps.docker_cache.outputs.proxy-log-path }}
          BORINGCACHE_PROXY_PORT: ${{ steps.docker_cache.outputs.proxy-port }}
          BUILDER: ${{ steps.docker_cache.outputs.buildx-name }}
          CACHE_FROM: ${{ steps.docker_cache.outputs.cache-from }}
          CACHE_TO: ${{ steps.docker_cache.outputs.cache-to }}
          CACHE_SCOPE: ${{ steps.scope.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM_FRONTEND: ${{ steps.scope.outputs.frontend_pnpm_tag }}
          BORINGCACHE_TAG_PNPM_PLUGIN: ${{ steps.scope.outputs.plugin_pnpm_tag }}
          BORINGCACHE_TAG_TURBO: ${{ steps.scope.outputs.turbo_tag }}
          BORINGCACHE_TAG_TURBO_PLUGIN: ${{ steps.scope.outputs.turbo_plugin_tag }}
          BORINGCACHE_TAG_NODE_SCRIPTS: ${{ steps.scope.outputs.node_scripts_tag }}
          BORINGCACHE_TAG_PYTHON: ${{ steps.scope.outputs.python_tag }}
          BORINGCACHE_TAG_STATICFILES: ${{ steps.scope.outputs.staticfiles_tag }}
          BORINGCACHE_TAG_PLAYWRIGHT: ${{ steps.scope.outputs.playwright_tag }}
          IMAGE_TAG: posthog-benchmark:latest
        run: |
          set -euo pipefail
          metrics_file="${RUNNER_TEMP}/cold-build-metrics.env"
          rm -f "$metrics_file"
          start="$(date +%s)"
          BENCHMARK_METRICS_OUTPUT="$metrics_file" ./run-boringcache-build.sh ${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane == 'rolling' && 'full' || 'seed-cache' }}
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"
          if [[ -s "$metrics_file" ]]; then
            cat "$metrics_file" >> "$GITHUB_OUTPUT"
          fi

  scenario-builds:
    name: Scenario (${{ matrix.phase }}) (BoringCache)
    needs: seed-cache
    runs-on: ubuntu-latest
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        phase: [warm1, layer_miss]
    env:
      BORINGCACHE_SAVE_TOKEN: ''
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          df -h /

      - name: Checkout benchmark repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          submodules: false

      - name: Checkout pinned upstream submodule shallowly
        run: |
          set -euo pipefail
          git submodule sync --recursive
          git -c protocol.version=2 submodule update --init --depth 1 --recommend-shallow --jobs 4 upstream

      - name: Prepare scenario source tree
        run: ./scripts/prepare-source.sh "${{ matrix.phase }}"

      - name: Install BoringCache CLI with boringcache/one
        uses: boringcache/one@v1
        with:
          setup: none
          mode: archive
          workspace: ${{ env.BENCHMARK_WORKSPACE }}
          cli-platform: linux-amd64
          cache-runtime: false

      - name: Prepare benchmark build inputs
        id: scope
        run: |
          cp "$(which boringcache)" ./boringcache-bin

          ref_slug="$(printf '%s' "${GITHUB_REF_NAME}" | tr -c 'A-Za-z0-9._-' '-')"
          ref_slug="${ref_slug#-}"
          ref_slug="${ref_slug%-}"
          ref_slug="${ref_slug:-main}"
          if [[ "${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane || 'fresh' }}" == "rolling" ]]; then
            run_suffix="rolling-${ref_slug}"
          else
            run_suffix="r${GITHUB_RUN_ID}-a${GITHUB_RUN_ATTEMPT}"
          fi
          cache_scope="${BENCHMARK_ID}-run-${run_suffix}"
          docker_registry_tag="${BENCHMARK_ID}-docker-layers-${run_suffix}"
          frontend_pnpm_tag="posthog-pnpm-frontend-${run_suffix}"
          plugin_pnpm_tag="posthog-pnpm-plugin-${run_suffix}"
          case "${TURBO_BACKEND}" in
            archive)
              turbo_tag="posthog-turbo-archive-${run_suffix}"
              turbo_plugin_tag="posthog-turbo-plugin-archive-${run_suffix}"
              ;;
            proxy|off)
              turbo_tag="posthog-turbo-${run_suffix}"
              turbo_plugin_tag="posthog-turbo-plugin-${run_suffix}"
              ;;
            *)
              echo "Unknown TURBO_BACKEND: ${TURBO_BACKEND}" >&2
              exit 1
              ;;
          esac
          node_scripts_tag="posthog-node-scripts-${run_suffix}"
          python_tag="posthog-python-${run_suffix}"
          staticfiles_tag="posthog-staticfiles-${run_suffix}"
          playwright_tag="posthog-playwright-${run_suffix}"
          tags_csv="${frontend_pnpm_tag},${plugin_pnpm_tag},${turbo_tag},${turbo_plugin_tag},${node_scripts_tag},${python_tag},${staticfiles_tag},${playwright_tag},${cache_scope},${docker_registry_tag}"
          registry_proxy_tags="${cache_scope},${docker_registry_tag}"
          echo "cache_scope=${cache_scope}" >> "$GITHUB_OUTPUT"
          echo "docker_registry_tag=${docker_registry_tag}" >> "$GITHUB_OUTPUT"
          echo "registry_proxy_tags=${registry_proxy_tags}" >> "$GITHUB_OUTPUT"
          echo "frontend_pnpm_tag=${frontend_pnpm_tag}" >> "$GITHUB_OUTPUT"
          echo "plugin_pnpm_tag=${plugin_pnpm_tag}" >> "$GITHUB_OUTPUT"
          echo "turbo_tag=${turbo_tag}" >> "$GITHUB_OUTPUT"
          echo "turbo_plugin_tag=${turbo_plugin_tag}" >> "$GITHUB_OUTPUT"
          echo "node_scripts_tag=${node_scripts_tag}" >> "$GITHUB_OUTPUT"
          echo "python_tag=${python_tag}" >> "$GITHUB_OUTPUT"
          echo "staticfiles_tag=${staticfiles_tag}" >> "$GITHUB_OUTPUT"
          echo "playwright_tag=${playwright_tag}" >> "$GITHUB_OUTPUT"
          echo "tags_csv=${tags_csv}" >> "$GITHUB_OUTPUT"
          cat > ./run-boringcache-build.sh <<'SH'
          #!/usr/bin/env bash
          set -euo pipefail

          proxy_port="${BORINGCACHE_PROXY_PORT:-5000}"
          proxy_log="${BORINGCACHE_PROXY_LOG_PATH:-/tmp/boringcache-proxy-${proxy_port}.log}"
          build_log="$(mktemp /tmp/boringcache-build.XXXXXX.log)"
          max_attempts=3
          cache_export_pattern='expected sha256:.*got sha256:e3b0|error writing layer blob|400 Bad Request|broken pipe'

          mode="${1:-full}"
          start_proxy() { :; }
          stop_proxy() { :; }
          wait_proxy_ready() { return 0; }
          cleanup() { :; }
          trap cleanup EXIT

          find_step_id() {
            local pattern="$1"
            sed -nE "s/^#([0-9]+) ${pattern}.*/\\1/p" "$build_log" | tail -n1
          }

          find_step_seconds() {
            local step_id="$1"
            [[ -n "$step_id" ]] || return 0
            sed -nE "s/^#${step_id} DONE ([0-9]+(\\.[0-9]+)?)s$/\\1/p" "$build_log" | tail -n1
          }

          write_build_metrics() {
            local output_path="${BENCHMARK_METRICS_OUTPUT:-}"
            [[ -n "$output_path" ]] || return 0

            local import_step=""
            local export_step=""
            local import_seconds=""
            local export_seconds=""

            import_step="$(find_step_id "importing cache manifest from")"
            export_step="$(find_step_id "exporting cache to registry")"
            import_seconds="$(find_step_seconds "$import_step")"
            export_seconds="$(find_step_seconds "$export_step")"

            mkdir -p "$(dirname "$output_path")"
            : > "$output_path"
            if [[ -n "$import_seconds" ]]; then
              echo "docker_cache_import_seconds=$import_seconds" >> "$output_path"
            fi
            if [[ -n "$export_seconds" ]]; then
              echo "docker_cache_export_seconds=$export_seconds" >> "$output_path"
            fi
          }

          attempt=1
          while true; do
            cache_args=()
            internal_restore_enabled="1"
            internal_save_enabled="1"
            turbo_proxy_enabled="${BORINGCACHE_TURBO_PROXY_ENABLED:-1}"
            if [[ "$mode" == "full" ]]; then
              [[ -n "${CACHE_FROM:-}" ]] && cache_args+=(--cache-from "$CACHE_FROM")
              [[ -n "${CACHE_TO:-}" ]] && cache_args+=(--cache-to "$CACHE_TO")
            elif [[ "$mode" == "seed-cache" ]]; then
              # --no-cache is required for type=registry export: without it, buildx
              # sees cached layers from the builder and skips pushing blobs to the
              # registry proxy, so the proxy never uploads to BC backend.
              cache_args=(--no-cache)
              [[ -n "${CACHE_TO:-}" ]] && cache_args+=(--cache-to "$CACHE_TO")
            elif [[ "$mode" == "partial-warm" ]]; then
              # Read-only: no --cache-to. Internal saves are gated by an empty
              # save-token secret so the Dockerfile instruction keys stay stable.
              [[ -n "${CACHE_FROM:-}" ]] && cache_args+=(--cache-from "$CACHE_FROM")
            elif [[ "$mode" == "internal-only" ]]; then
              # Disable Docker layer cache for a full stale rebuild.
              cache_args=(--no-cache)
            elif [[ "$mode" == "internal-target" ]]; then
              # Isolate internal dependency/output cache impact without final-image OS package work.
              cache_args=(--no-cache --target posthog-build)
            else
              echo "Unknown build mode: $mode" >&2
              exit 1
            fi
            start_proxy
            if ! wait_proxy_ready; then
              echo "Registry proxy did not become ready (attempt ${attempt}/${max_attempts})" >&2
              tail -n 200 "$proxy_log" || true
              if [[ "$attempt" -ge "$max_attempts" ]]; then
                exit 1
              fi
              stop_proxy
              attempt=$((attempt + 1))
              sleep 3
              continue
            fi

            : > "$build_log"
            set +e
            DOCKER_BUILDKIT=1 docker buildx build \
              --builder "$BUILDER" \
              --file Dockerfile.benchmark \
              --tag "$IMAGE_TAG" \
              --progress=plain \
              "${cache_args[@]}" \
              --secret id=boringcache_token,env=BORINGCACHE_RESTORE_TOKEN \
              --secret id=boringcache_save_token,env=BORINGCACHE_SAVE_TOKEN \
              --build-arg BORINGCACHE_WORKSPACE="$BENCHMARK_WORKSPACE" \
              --build-arg BORINGCACHE_TAG_PNPM_FRONTEND="$BORINGCACHE_TAG_PNPM_FRONTEND" \
              --build-arg BORINGCACHE_TAG_PNPM_PLUGIN="$BORINGCACHE_TAG_PNPM_PLUGIN" \
              --build-arg BORINGCACHE_TAG_TURBO="$BORINGCACHE_TAG_TURBO" \
              --build-arg BORINGCACHE_TAG_TURBO_PLUGIN="$BORINGCACHE_TAG_TURBO_PLUGIN" \
              --build-arg BORINGCACHE_TAG_NODE_SCRIPTS="$BORINGCACHE_TAG_NODE_SCRIPTS" \
              --build-arg BORINGCACHE_TAG_PYTHON="$BORINGCACHE_TAG_PYTHON" \
              --build-arg BORINGCACHE_TAG_STATICFILES="$BORINGCACHE_TAG_STATICFILES" \
              --build-arg BORINGCACHE_TAG_PLAYWRIGHT="$BORINGCACHE_TAG_PLAYWRIGHT" \
              --build-arg BORINGCACHE_INTERNAL_RESTORE_ENABLED="$internal_restore_enabled" \
              --build-arg BORINGCACHE_INTERNAL_SAVE_ENABLED="$internal_save_enabled" \
              --build-arg BORINGCACHE_TURBO_PROXY_ENABLED="$turbo_proxy_enabled" \
              --build-arg BORINGCACHE_TURBO_BACKEND="${TURBO_BACKEND}" \
              . 2>&1 | tee "$build_log"
            status=${PIPESTATUS[0]}
            set -e

            if [[ "$status" -eq 0 ]]; then
              if [[ "$mode" =~ ^(full|seed-cache)$ ]] && grep -Eq "$cache_export_pattern" "$build_log"; then
                stop_proxy
                if [[ "$attempt" -lt "$max_attempts" ]]; then
                  echo "Build succeeded but cache export was degraded (attempt ${attempt}/${max_attempts}); retrying to self-heal cache..." >&2
                  attempt=$((attempt + 1))
                  sleep 3
                  continue
                fi
                echo "Build succeeded but cache export still degraded after ${max_attempts} attempts; continuing run with best-effort cache state." >&2
              fi
              # Stop proxy gracefully so it can flush pending uploads
              if [[ "$mode" =~ ^(seed-cache|full)$ ]]; then
                echo "Flushing proxy cache to backend..."
                stop_proxy
              fi
              # Dump proxy log for diagnostics
              echo "=== Proxy log (${mode}, last 200 lines) ==="
              tail -n 200 "$proxy_log" 2>/dev/null || true
              echo "=== End proxy log ==="
              write_build_metrics
              break
            fi

            stop_proxy

            if [[ "$attempt" -ge "$max_attempts" ]]; then
              echo "Build (${mode}) failed after ${max_attempts} attempts" >&2
              tail -n 200 "$build_log" || true
              tail -n 400 "$proxy_log" || true
              exit "$status"
            fi

            if grep -Eq "$cache_export_pattern" "$build_log"; then
              echo "Detected registry blob finalize mismatch (attempt ${attempt}/${max_attempts}); restarting proxy and retrying..." >&2
            else
              echo "Build (${mode}) failed (attempt ${attempt}/${max_attempts}); retrying..." >&2
            fi

            attempt=$((attempt + 1))
            sleep 3
          done
          SH
          chmod +x ./run-boringcache-build.sh

      - name: Configure boringcache/one for Docker
        id: docker_cache
        uses: boringcache/one@v1
        with:
          cli-version: skip
          setup: none
          mode: docker
          docker-command: setup
          workspace: ${{ env.BENCHMARK_WORKSPACE }}
          cache-tag: ${{ needs.seed-cache.outputs.cache_scope }}
          registry-tag: ${{ steps.scope.outputs.registry_proxy_tags }}
          proxy-no-git: 'true'
          read-only: true
          driver-opts: |
            network=host
          verbose: true
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}

      - name: Prune builder cache for isolated scenario
        run: docker buildx prune -f --builder "${{ steps.docker_cache.outputs.buildx-name }}"
      - name: Run warm build 1
        id: warm1_build
        if: matrix.phase == 'warm1'
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_PROXY_LOG_PATH: ${{ steps.docker_cache.outputs.proxy-log-path }}
          BORINGCACHE_PROXY_PORT: ${{ steps.docker_cache.outputs.proxy-port }}
          BUILDER: ${{ steps.docker_cache.outputs.buildx-name }}
          CACHE_FROM: ${{ steps.docker_cache.outputs.cache-from }}
          CACHE_TO: ${{ steps.docker_cache.outputs.cache-to }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM_FRONTEND: ${{ steps.scope.outputs.frontend_pnpm_tag }}
          BORINGCACHE_TAG_PNPM_PLUGIN: ${{ steps.scope.outputs.plugin_pnpm_tag }}
          BORINGCACHE_TAG_TURBO: ${{ steps.scope.outputs.turbo_tag }}
          BORINGCACHE_TAG_TURBO_PLUGIN: ${{ steps.scope.outputs.turbo_plugin_tag }}
          BORINGCACHE_TAG_NODE_SCRIPTS: ${{ steps.scope.outputs.node_scripts_tag }}
          BORINGCACHE_TAG_PYTHON: ${{ steps.scope.outputs.python_tag }}
          BORINGCACHE_TAG_STATICFILES: ${{ steps.scope.outputs.staticfiles_tag }}
          BORINGCACHE_TAG_PLAYWRIGHT: ${{ steps.scope.outputs.playwright_tag }}
          IMAGE_TAG: posthog-benchmark:latest
        run: |
          set -euo pipefail
          metrics_file="${RUNNER_TEMP}/warm1-build-metrics.env"
          rm -f "$metrics_file"
          start="$(date +%s)"
          BENCHMARK_METRICS_OUTPUT="$metrics_file" ./run-boringcache-build.sh partial-warm
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"
          if [[ -s "$metrics_file" ]]; then
            cat "$metrics_file" >> "$GITHUB_OUTPUT"
          fi

      - name: Run warm build 2
        id: warm2_build
        if: matrix.phase == 'warm2'
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_PROXY_LOG_PATH: ${{ steps.docker_cache.outputs.proxy-log-path }}
          BORINGCACHE_PROXY_PORT: ${{ steps.docker_cache.outputs.proxy-port }}
          BUILDER: ${{ steps.docker_cache.outputs.buildx-name }}
          CACHE_FROM: ${{ steps.docker_cache.outputs.cache-from }}
          CACHE_TO: ${{ steps.docker_cache.outputs.cache-to }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM_FRONTEND: ${{ steps.scope.outputs.frontend_pnpm_tag }}
          BORINGCACHE_TAG_PNPM_PLUGIN: ${{ steps.scope.outputs.plugin_pnpm_tag }}
          BORINGCACHE_TAG_TURBO: ${{ steps.scope.outputs.turbo_tag }}
          BORINGCACHE_TAG_TURBO_PLUGIN: ${{ steps.scope.outputs.turbo_plugin_tag }}
          BORINGCACHE_TAG_NODE_SCRIPTS: ${{ steps.scope.outputs.node_scripts_tag }}
          BORINGCACHE_TAG_PYTHON: ${{ steps.scope.outputs.python_tag }}
          BORINGCACHE_TAG_STATICFILES: ${{ steps.scope.outputs.staticfiles_tag }}
          BORINGCACHE_TAG_PLAYWRIGHT: ${{ steps.scope.outputs.playwright_tag }}
          IMAGE_TAG: posthog-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh partial-warm
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Run stale-low build (committed source change, partial warm)
        id: stale_low_build
        if: matrix.phase == 'stale-low'
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_PROXY_LOG_PATH: ${{ steps.docker_cache.outputs.proxy-log-path }}
          BORINGCACHE_PROXY_PORT: ${{ steps.docker_cache.outputs.proxy-port }}
          BUILDER: ${{ steps.docker_cache.outputs.buildx-name }}
          CACHE_FROM: ${{ steps.docker_cache.outputs.cache-from }}
          CACHE_TO: ${{ steps.docker_cache.outputs.cache-to }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM_FRONTEND: ${{ steps.scope.outputs.frontend_pnpm_tag }}
          BORINGCACHE_TAG_PNPM_PLUGIN: ${{ steps.scope.outputs.plugin_pnpm_tag }}
          BORINGCACHE_TAG_TURBO: ${{ steps.scope.outputs.turbo_tag }}
          BORINGCACHE_TAG_TURBO_PLUGIN: ${{ steps.scope.outputs.turbo_plugin_tag }}
          BORINGCACHE_TAG_NODE_SCRIPTS: ${{ steps.scope.outputs.node_scripts_tag }}
          BORINGCACHE_TAG_PYTHON: ${{ steps.scope.outputs.python_tag }}
          BORINGCACHE_TAG_STATICFILES: ${{ steps.scope.outputs.staticfiles_tag }}
          BORINGCACHE_TAG_PLAYWRIGHT: ${{ steps.scope.outputs.playwright_tag }}
          IMAGE_TAG: posthog-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh partial-warm
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Run stale-mid build (committed frontend package descriptor change, partial warm)
        id: stale_mid_build
        if: matrix.phase == 'stale-mid'
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_PROXY_LOG_PATH: ${{ steps.docker_cache.outputs.proxy-log-path }}
          BORINGCACHE_PROXY_PORT: ${{ steps.docker_cache.outputs.proxy-port }}
          BUILDER: ${{ steps.docker_cache.outputs.buildx-name }}
          CACHE_FROM: ${{ steps.docker_cache.outputs.cache-from }}
          CACHE_TO: ${{ steps.docker_cache.outputs.cache-to }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM_FRONTEND: ${{ steps.scope.outputs.frontend_pnpm_tag }}
          BORINGCACHE_TAG_PNPM_PLUGIN: ${{ steps.scope.outputs.plugin_pnpm_tag }}
          BORINGCACHE_TAG_TURBO: ${{ steps.scope.outputs.turbo_tag }}
          BORINGCACHE_TAG_TURBO_PLUGIN: ${{ steps.scope.outputs.turbo_plugin_tag }}
          BORINGCACHE_TAG_NODE_SCRIPTS: ${{ steps.scope.outputs.node_scripts_tag }}
          BORINGCACHE_TAG_PYTHON: ${{ steps.scope.outputs.python_tag }}
          BORINGCACHE_TAG_STATICFILES: ${{ steps.scope.outputs.staticfiles_tag }}
          BORINGCACHE_TAG_PLAYWRIGHT: ${{ steps.scope.outputs.playwright_tag }}
          IMAGE_TAG: posthog-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh partial-warm
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Run layer-miss build (no Docker cache, full image)
        id: internal_only_build
        if: matrix.phase == 'layer_miss'
        env:
          BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
          BORINGCACHE_PROXY_LOG_PATH: ${{ steps.docker_cache.outputs.proxy-log-path }}
          BORINGCACHE_PROXY_PORT: ${{ steps.docker_cache.outputs.proxy-port }}
          BUILDER: ${{ steps.docker_cache.outputs.buildx-name }}
          CACHE_FROM: ${{ steps.docker_cache.outputs.cache-from }}
          CACHE_TO: ${{ steps.docker_cache.outputs.cache-to }}
          CACHE_SCOPE: ${{ needs.seed-cache.outputs.cache_scope }}
          BORINGCACHE_TAG_PNPM_FRONTEND: ${{ steps.scope.outputs.frontend_pnpm_tag }}
          BORINGCACHE_TAG_PNPM_PLUGIN: ${{ steps.scope.outputs.plugin_pnpm_tag }}
          BORINGCACHE_TAG_TURBO: ${{ steps.scope.outputs.turbo_tag }}
          BORINGCACHE_TAG_TURBO_PLUGIN: ${{ steps.scope.outputs.turbo_plugin_tag }}
          BORINGCACHE_TAG_NODE_SCRIPTS: ${{ steps.scope.outputs.node_scripts_tag }}
          BORINGCACHE_TAG_PYTHON: ${{ steps.scope.outputs.python_tag }}
          BORINGCACHE_TAG_STATICFILES: ${{ steps.scope.outputs.staticfiles_tag }}
          BORINGCACHE_TAG_PLAYWRIGHT: ${{ steps.scope.outputs.playwright_tag }}
          IMAGE_TAG: posthog-benchmark:latest
        run: |
          set -euo pipefail
          start="$(date +%s)"
          ./run-boringcache-build.sh internal-only
          end="$(date +%s)"
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      - name: Capture scenario timing
        id: phase_timing
        run: |
          set -euo pipefail
          seconds=""
          docker_cache_import_seconds=""
          case "${{ matrix.phase }}" in
            warm1)
              seconds="${{ steps.warm1_build.outputs.seconds }}"
              docker_cache_import_seconds="${{ steps.warm1_build.outputs.docker_cache_import_seconds }}"
              ;;
            warm2)
              seconds="${{ steps.warm2_build.outputs.seconds }}"
              ;;
            stale-low)
              seconds="${{ steps.stale_low_build.outputs.seconds }}"
              ;;
            stale-mid)
              seconds="${{ steps.stale_mid_build.outputs.seconds }}"
              ;;
            layer_miss)
              seconds="${{ steps.internal_only_build.outputs.seconds }}"
              ;;
            *)
              echo "Unknown phase: ${{ matrix.phase }}" >&2
              exit 1
              ;;
          esac
          if [[ -z "${seconds}" ]]; then
            echo "No timing captured for phase ${{ matrix.phase }}" >&2
            exit 1
          fi
          echo "seconds=${seconds}" >> "$GITHUB_OUTPUT"
          if [[ -n "${docker_cache_import_seconds}" ]]; then
            echo "docker_cache_import_seconds=${docker_cache_import_seconds}" >> "$GITHUB_OUTPUT"
          fi
      - name: Persist scenario timing artifact
        run: |
          set -euo pipefail
          mkdir -p benchmark-phase
          cat > "benchmark-phase/${{ matrix.phase }}.env" <<EOF
          phase=${{ matrix.phase }}
          seconds=${{ steps.phase_timing.outputs.seconds }}
          docker_cache_import_seconds=${{ steps.phase_timing.outputs.docker_cache_import_seconds }}
          EOF
      - name: Upload scenario timing artifact
        uses: actions/upload-artifact@v6
        with:
          name: benchmark-phase-${{ env.BENCHMARK_ID }}-boringcache-${{ matrix.phase }}
          path: benchmark-phase/${{ matrix.phase }}.env
          if-no-files-found: error

  finalize-benchmark:
    name: Finalize benchmark artifacts
    needs: [seed-cache, scenario-builds]
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Checkout benchmark repo helpers
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          path: .benchmark-config

      - name: Install BoringCache CLI with boringcache/one
        uses: boringcache/one@v1
        with:
          setup: none
          mode: archive
          workspace: ${{ env.BENCHMARK_WORKSPACE }}
          cli-platform: linux-amd64
          cache-runtime: false

      - name: Download scenario timing artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: benchmark-phase-${{ env.BENCHMARK_ID }}-boringcache-*
          path: benchmark-phase
          merge-multiple: true
      - name: Collect scenario timings
        id: timings
        run: |
          set -euo pipefail
          parse_phase() {
            local phase="$1"
            local file="benchmark-phase/${phase}.env"
            if [[ ! -f "$file" ]]; then
              echo "Missing timing artifact for phase ${phase}" >&2
              ls -R benchmark-phase || true
              exit 1
            fi
            local seconds
            seconds="$(grep "^seconds=" "$file" | head -n1 | cut -d= -f2-)"
            if [[ -z "${seconds}" ]]; then
              echo "Timing artifact for phase ${phase} has empty seconds" >&2
              cat "$file" >&2
              exit 1
            fi
            local key="${phase//-/_}"
            echo "${key}_seconds=${seconds}" >> "$GITHUB_OUTPUT"
            local docker_cache_import_seconds
            docker_cache_import_seconds="$(grep "^docker_cache_import_seconds=" "$file" | head -n1 | cut -d= -f2- || true)"
            if [[ -n "${docker_cache_import_seconds}" ]]; then
              echo "${key}_docker_cache_import_seconds=${docker_cache_import_seconds}" >> "$GITHUB_OUTPUT"
            fi
          }
          parse_phase warm1
          parse_phase layer_miss
      - name: Measure BoringCache storage usage
        id: cache_size
        env:
          BORINGCACHE_API_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}
        run: |
          tags_csv="${{ needs.seed-cache.outputs.tags_csv }}"
          bytes="$(.benchmark-config/scripts/sum-boringcache-check-sizes.sh "$BENCHMARK_WORKSPACE" "$tags_csv")"
          echo "bytes=${bytes}" >> "$GITHUB_OUTPUT"

      - name: Capture committed upstream ref
        id: upstream_ref
        run: echo "sha=$(git -C .benchmark-config rev-parse HEAD:upstream)" >> "$GITHUB_OUTPUT"

      - name: Write benchmark artifacts
        id: artifact_files
        run: |
          .benchmark-config/scripts/write-benchmark-artifacts.sh \
            --benchmark "$BENCHMARK_ID" \
            --strategy "boringcache" \
            --lane "${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane || 'fresh' }}" \
            --project-repo "$PROJECT_REPO" \
            --project-ref "${{ steps.upstream_ref.outputs.sha }}" \
            --cold-seconds "${{ needs.seed-cache.outputs.cold_seconds }}" \
            --warm1-seconds "${{ steps.timings.outputs.warm1_seconds }}" \
            --layer-miss-seconds "${{ steps.timings.outputs.layer_miss_seconds }}" \
            --docker-cache-import-seconds "${{ steps.timings.outputs.warm1_docker_cache_import_seconds }}" \
            --docker-cache-export-seconds "${{ needs.seed-cache.outputs.docker_cache_export_seconds }}" \
            --cache-storage-bytes "${{ steps.cache_size.outputs.bytes }}" \
            --cache-storage-source "boringcache-check" \
            --bytes-uploaded "${{ steps.cache_size.outputs.bytes }}" \
            --hit-behavior-note "Source tree comes from the benchmark repo's committed upstream submodule. Cold+seed starts from empty BC tags and empty Docker layer cache, uses --no-cache + cache-to via the BC registry proxy (mode=max), and records cold timing while priming the shared cache. Warm1 rebuilds the same pinned base tree on a fresh runner with cache-from only, internal restore enabled, and no synthetic mutations."

      - name: Upload benchmark artifacts
        uses: actions/upload-artifact@v6
        with:
          name: benchmark-${{ env.BENCHMARK_ID }}-boringcache-${{ github.event_name == 'workflow_dispatch' && inputs.cache_lane || 'fresh' }}
          path: |
            ${{ steps.artifact_files.outputs.json_path }}
            ${{ steps.artifact_files.outputs.md_path }}
          if-no-files-found: error

      - name: Publish summary
        run: cat "${{ steps.artifact_files.outputs.md_path }}" >> "$GITHUB_STEP_SUMMARY"

      - name: Storage comparison with GHA cache
        if: always() && steps.cache_size.outputs.bytes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          bc_bytes="${{ steps.cache_size.outputs.bytes }}"

          # Query GHA cache usage for this repo
          gha_bytes="$(gh api "repos/${GITHUB_REPOSITORY}/actions/cache/usage" --jq '.active_caches_size_in_bytes' 2>/dev/null)" || {
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "_Storage comparison skipped (GHA API unreachable)_" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          }

          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### Storage comparison" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Cache | Storage | Notes |" >> "$GITHUB_STEP_SUMMARY"
          echo "|---|---|---|" >> "$GITHUB_STEP_SUMMARY"
          echo "| BoringCache | $(numfmt --to=iec-i --suffix=B "${bc_bytes}") | PostHog benchmark tags (content-addressed, deduped across branches) |" >> "$GITHUB_STEP_SUMMARY"
          echo "| GitHub Actions | $(numfmt --to=iec-i --suffix=B "${gha_bytes}") | Repo total (buildkit blobs shared across projects, no cross-branch dedup) |" >> "$GITHUB_STEP_SUMMARY"
